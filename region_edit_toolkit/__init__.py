"""Private implementation modules for ComfyUI Region Edit Toolkit.

The Smart and Face/Object algorithm sources remain in separate modules because
some same-named helpers have different contracts.  These private classes are
implementation sources only; the public ComfyUI registry exposes only the
canonical ``RegionEdit...`` node IDs.
"""
